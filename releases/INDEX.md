# LumenCraft release archive

This directory contains the LumenCraft Bedrock resource packs, companions, and complete installers created during the project. Each archive was added in its own commit. The Poggy'sLum pack is intentionally excluded.

Use a `.mcpack` for a single resource or behavior pack and a `.mcaddon` for the combined installer when available. Do not rename extensions.
